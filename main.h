#ifndef MAIN_H
#define MAIN_H

#include <unistd.h>
#include <stdlib.h>
#include <stdarg.h>
#include <stdio.h>

int	_putnbr(int n);
int _binary(int nbr);
int	_putchar(char c);
int	_putstr(char *str);
int _printf(const char *format, ...);

#endif
