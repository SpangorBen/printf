#ifndef MAIN_H
#define MAIN_H

#include <unistd.h>
#include <stdlib.h>
#include <stdarg.h>

int	_putnbr(int n);
int	_putchar(char c);
int	_putstr(char *str);
int _printf(const char *format, ...);

#endif
